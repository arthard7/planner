class DASHBOARD {
    private root = '/i'

    HOME = this.root
    TASKS = `${this.root}/tasks`
    HABITS = `${this.root}/habits`
    TIMER = `${this.root}/timer`
    TIMER_BLOCKING = `${this.root}/time-blocking`
    SETTINGS = `${this.root}/settings`

}

export const DASHBOARDS_PAGES = new DASHBOARD()